#include <stdio.h>
void main() {
	int num1 = 15, num2 = 25, sum;
	printf("Given integers are num1 = %d, num2 = %d\n", num1, num2);
	//Write the code to add num1 and num2 and place the result in the variable sum
	sum=num1+num2;
	printf("Sum of 2 given numbers = %d\n", sum);
}