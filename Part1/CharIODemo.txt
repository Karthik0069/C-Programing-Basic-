#include <stdio.h>
void main() {
	char ch;
	printf("Enter a character : ");
	ch = getchar(); // Use a function to read a character
	printf("The entered character is : ");
	putchar(ch);
	// Use a function to print the character
}